<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
</head>
<body>
    <h1> URL Shortner </h1>
    <%if(locals.id) {%>
        <p> URL Genrated: http://localhost:8001/url/<%=id%> </p>
        <%}%>
    <div>
        <form method="post" action="/url">
            <label> Enter your Orignal URL </label>
            <input type="text" name = "url" placeholder="org url" />
            <button type="submit">Genrate </button>
            </form>
        </div>
    <div>
        <%if(locals.urls) { %>
            <table>
                <thread>
                    <th>S.no </th>
                    <th>ShortID </th>
                    <th>Redirect </th>
                    <th>Clicks </th>
                    </thread>
                <tbody>
                    <%urls.forEach((url, index) => {%>
                        <tr>
                        <td><%= index + 1%> </td>
                        <td><%=url.shortId%> </td>
                        <td><%=url.redirectURL%> </td>
                        <td><%=url.visitHistory.length%> </td>
                        </tr>
                        <%})%>
                    </tbody>
                </table>
            <%}%>
        </div>
</body>
</html>